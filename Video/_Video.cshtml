@inherits Custom.Hybrid.Razor12
@using ToSic.Razor.Blade;
@{
  /* This Template is used in ca. ??? Views which show title, text and video */
 
  // Determine the widths of each part, based on 12 Bootstrap Cols
  const int bootstrapCols = 12;
  var colsText = Settings.TextFirst ? Settings.ColsElement1 : bootstrapCols - Settings.ColsElement1;
  var colsImg = bootstrapCols - colsText;
}
<div class="co-container-outer mb-4 mb-md-5">
  <div class="co-container-inner row" @Edit.TagToolbar(Content)>
    @* <div class="row"> *@
      <div class='col-md-@colsText order-@(Settings.TextFirst ? "first" : "last")'>
        @* call sub-template to show the title with the correct H1-H6 tag *@
        @Html.Partial("../shared/_Header tag.cshtml")
        @Html.Raw(Content.Text)
      </div>
      <div class='col-md-@colsImg order-@(Settings.TextFirst ? "last" : "first")'>
        @Html.Partial("_Video-Part.cshtml", new { width = 640, height = 360 })
      </div>
    @* </div> *@
  </div>
</div>

@Html.Partial("../shared/_Assets.cshtml", new { parts = "styles,fancybox,icons"})