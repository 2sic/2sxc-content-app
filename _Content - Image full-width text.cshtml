@using Connect.Koi

@* put all necessary link/script tags here *@
@RenderPage("shared/_Assets.cshtml", new { parts = "fancybox,styles,scripts"})

<div class="co-container-outer">
	<div class="co-container-inner clearfix sc-element co-content co-content-image-left co-text-beside-image mb-4">
		<div class="row">
			@Edit.Toolbar(Content)
			<div @Koi.Class("bs3='col col-sm-12' bs4,oth='col-md-12'")>
				@* call sub-template to show the title with the correct H1-H6 tag *@
				@RenderPage("shared/_Header tag.cshtml")

				@* call sub-template to show the image with all possible options *@
				@RenderPage("shared/_Image and optional Lightbox.cshtml", new { img = Content, width = 700 })

				<div class="co-text">
					@Html.Raw(Content.Text)
				</div>
			</div>
		</div>
	</div>
</div>