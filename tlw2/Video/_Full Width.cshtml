@inherits ToSic.Sxc.Dnn.RazorComponent
<div class="max-w-full mb-10 md:mb-16">
  <div class="grid grid-cols-1 text-left" @Edit.TagToolbar(Content)>
    <div class="mb-2 text-2xl lg:text-3xl">
      @* call sub-template to show the title with the correct H1-H6 tag *@
      @RenderPage("../shared/_Header tag.cshtml")
    </div>
    <div class="mb-2">
      @RenderPage("_Video-Part.cshtml", new { width = 1280, height = 640 })
    </div>
    <div>
      @Html.Raw(Content.Text)
    </div>
  </div>
</div>

@RenderPage("../shared/_Assets.cshtml", new { parts = "styles,scripts,fancybox,icons"})